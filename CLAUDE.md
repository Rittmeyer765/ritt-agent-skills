@AGENTS.md

# Claude-specific notes

- Reload the plugin after changing manifests or skill frontmatter.
- Run `claude plugin validate . --strict` when available, in addition to `scripts/validate_repo.py`.
- Treat Markdown as documentation, not as a technical security boundary.
- Never replace the manual upstream review process with automatic marketplace updates.
